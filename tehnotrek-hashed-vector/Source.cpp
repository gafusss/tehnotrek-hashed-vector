/*Привет! В ДЗ3 тебе надо реализовать контейнер hashed_vector. Основная цель - искать в контейнере за О(1')(hash) и получать элемент по индексу за О(1)(vector). Внешний интерфейс должен совпадать c классическим vector'ом, только метод find должен работать значительно быcтрее классического.
----
Вячеслав Ишутин
email: v.ishutin@corp.mail.ru
Преподаватель Технотрек@Mail.ru
*/

int main(int argc, char * argv[], char * env[])
{
	return 0;
}